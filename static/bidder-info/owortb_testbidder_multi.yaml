# sample bidder-info yaml for testbidder (oRTB Integration)
maintainer:
  email: "header-bidding@pubmatic.com"
endpoint: "http://{{.host}}.endpoint.com/zone={{.zone}}"
extra_info: "{\"requestMode\":\"multi\"}" # prepares multiple requests, each request contains single impression
capabilities:
  app:
    mediaTypes:
      - video
  site:
    mediaTypes:
      - video
